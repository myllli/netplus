// 이 파일은 비활성화되었습니다.
// 테스트가 필요할 경우 파일 이름을 widget_test.dart로 변경하세요.

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:netplus/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    print('테스트 시작');

    await tester.pumpWidget(
      const MaterialApp(
        home: Scaffold(
          body: Center(
            child: Text('NetPlus'),
          ),
        ),
      ),
    );

    print('위젯 렌더링 완료');

    expect(find.text('NetPlus'), findsOneWidget);

    print('테스트 성공');
  });
}
